@using System.Collections
@using EIST.Web.Models
@model IEnumerable<EIST.Entities.Project>

@{
    ViewBag.Title = "Project";
    var sl = 1;
}


<div class="content">
    <div class="card border-teal">
        <div class="card-header bg-teal header-elements-inline">
            <h3 class="card-title"><strong>Project</strong></h3>
            <div class="header-elements">
                <div class="list-icons">
                    <a href="@Url.Action("Add","Project")" class="list-icons-item text-primary-600"><i class="icon-plus3"></i></a>
                </div>
            </div>
        </div>
        <hr class="hr-mar-reduce" />
        <div class="card-body">
            <div class="row">
                <div class="col-md-12">
                    <div class="table-responsive">
                        <table class="table table-bordered table-striped table-condensed">
                            <thead>
                                <tr>
                                    <th class="serial">#</th>
                                    <th>@Html.DisplayNameFor(model => model.Name)</th>
                                    <th>@Html.DisplayNameFor(model => model.Description)</th>
                                    <th>@Html.DisplayNameFor(model => model.CompanyId)</th>
                                    <th>@Html.DisplayNameFor(model => model.PmId)</th>
                                    <th>@Html.DisplayNameFor(model => model.SuperVisorId)</th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td class="serial">@(sl++)</td>
                                        <td>@Html.DisplayFor(modelItem => item.Name)</td>
                                        <td>@Html.DisplayFor(modelItem => item.Description)</td>
                                        <td>@Html.DisplayFor(modelItem => item.Company.Name)</td>
                                        <td>@Html.DisplayFor(modelItem => item.ProjectManager.UserName)</td>
                                        <td>@Html.DisplayFor(modelItem => item.SuperVisor.UserName)</td>


                                        @*<td>
                                                <div class="list-icons">
                                                    <a href="@Url.Action("Edit","Project", new { id = item.Id })" class="list-icons-item text-primary-600"><i class="icon-pencil7"></i></a>
                                                    <a href="#" onclick="delete_Data('@item.Id')" class="list-icons-item text-danger-600"><i class="icon-trash"></i></a>
                                                    <a href="@Url.Action("Details","Project", new { id = item.Id })" target="_blank" title="Ticket Details" class="list-icons-item text-info-600"><i class="icon-eye"></i></a>

                                                </div>
                                            </td>*@
                                        <td>
                                            <div class="list-icons">
                                                <a href="#" onclick="details_Data('@item.Id')" class="list-icons-item text-info-600"><i class="icon-eye"></i></a>
                                                <a href="@Url.Action("Edit","Project", new { id = item.Id })" class="list-icons-item text-primary-600"><i class="icon-pencil7"></i></a>
                                                <a href="#" onclick="delete_Data('@item.Id')" class="list-icons-item text-danger-600"><i class="icon-trash"></i></a>

                                            </div>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>

            </div>

        </div>
    </div>
</div>

<!-- Details Modal -->
<div id="detailsModal" class="modal fade border-teal" tabindex="-1">
    <div class="modal-dialog modal-lg">
        <div class="modal-content border-teal">
            <div class="modal-header bg-teal-300">
                <h5 class="modal-title">Project Details</h5>
                <button type="button" class="close" data-dismiss="modal">&times;</button>
            </div>

            <div class="modal-body" id="detailsModalContent">

            </div>

            <div class="modal-footer">
                <button type="button" class="btn bg-teal-600" data-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>
<!-- Details Modal -->

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/jquery-confirm")
    @Scripts.Render("~/bundles/ajax-call")


    <script>

        function delete_Data(id) {
                var url = '@Url.Action("Delete", "Project")';
                var paramData = { id: id };
                delete_confirm(url, paramData);
        }

        function details_Data(id) {
            var url = '@Url.Action("Details", "Project")';
            var paramData = { id: id };
            ajaxCallModal(url, paramData, "renderProjectDetailsEntryLoad")

        }

        function renderProjectDetailsEntryLoad(data) {
            $("#detailsModalContent").html(data);
            $("#detailsModal").modal("show");
        }

        @*function getDataForEdit(id) {
            var url = '@Url.Action("GetCompanyProjectDetailsById", "Project")';
            var paramData = { id: id };
            ajaxCall(url, paramData, "renderCompanyProjectEntryLoad");
        }
        function renderCompanyProjectEntryLoad(data) {
            var editUrl = '@Url.Action("Edit", "Project")';
            $('#CompanyProjectCreateForm').attr('action', editUrl);
            $("#CompanyProjectId").val(data.Id);
            $("#InitialName").val(data.Name);
            $("#Name").val(data.Name);
            $("#Description").val(data.Description);
            $("#CompanyId").val(data.CompanyId);
             if (data.CompanyId == 0 || data.CompanyId == null) {
                $('#CompanyId option[value=""]').prop("selected", true);
            } else {
                $('#CompanyId option[value=' + data.CompanyId + ']').prop("selected", true);
            }
            $("#PmId").val(data.PmId);
             if (data.PmId == 0 || data.PmId == null) {
                $('#PmId option[value=""]').prop("selected", true);
            } else {
                $('#PmId option[value=' + data.PmId + ']').prop("selected", true);
            }
            $("#SuperVisorId").val(data.SuperVisorId);
             if (data.SuperVisorId == 0 || data.SuperVisorId == null) {
                $('#SuperVisorId option[value=""]').prop("selected", true);
            } else {
                $('#SuperVisorId option[value=' + data.SuperVisorId + ']').prop("selected", true);
            }

            $("#submitButton").html('<i class="fa fa-save"></i> Update');
        }*@
    </script>
}
